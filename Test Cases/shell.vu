import lexer

while True
    with file: File = open(input())
        result, error = lexer.run('<stdin>',file.read())
        if error
            print(error.as_string())
        else
            print(result)